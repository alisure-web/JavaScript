/**
 * Created by ialis on 2017/5/29.
 */

/**
 * 抽离配置数据
 *
 * 如：写死在代码中的字符串：
 *      URL、用户看到的字符串、重复的值、发生变更的值
 *
 * 需要将这些配置数据重代码中抽离：
 *      如定义一个config对象直接量。
 *
 * 最好将这些配置数据保存到另外的文件中：
 *      可以将之存放到属性文件中
 *      可以是：JSON、JSONP、JavaScript等
 * */

